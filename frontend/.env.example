# Vercel production should call the Render backend directly.
NEXT_PUBLIC_API_URL=https://metamax-un13.onrender.com

# Legacy local-only fallback. Keep this unset in Vercel production.
# NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
